# NETRA Host Security Agent (`netra-agent`)

The local NETRA Security Agent executable runs on host machines, performing vulnerability assessments, device enrollment, and secure telemetry reporting.

## CLI Usage

```bash
# Enroll agent with central security engine
netra enroll NETRA-ABCD-1234 --server-url http://localhost:4000/api/v1
```
